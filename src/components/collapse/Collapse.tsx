import React from 'react'

import { ICollapseProps } from './'

const Collapse = ({
  text
}: ICollapseProps) => {
  return (
    <div>
      <div>{text}</div>

    </div>
  )
}

export default Collapse